import 'package:flutter/material.dart';
class WheelSection extends StatelessWidget {
  const WheelSection({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}